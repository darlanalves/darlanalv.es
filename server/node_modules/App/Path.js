var rootPath = require('path').dirname(require.main.filename),
	appPath = rootPath + '/server',
	modPath = appPath + '/modules';

module.exports = {
	rootPath: rootPath,
	applicationPath: appPath,
	modulesPath: modPath
};